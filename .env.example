# Exemplo de variáveis de ambiente para produção (Render)
# Copie este arquivo para .env localmente (já está no .gitignore)
# No Render, configure estas variáveis na seção "Environment" do serviço

# Database (Render Postgres)
# Formato: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://mottuyard_user:password@dpg-xxxxx.onrender.com/mottuyard_db
SPRING_DATASOURCE_URL=${DATABASE_URL}
SPRING_DATASOURCE_USERNAME=mottuyard_user
SPRING_DATASOURCE_PASSWORD=your_secure_password

# OAuth2 GitHub (criar app em https://github.com/settings/developers)
SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_GITHUB_CLIENT_ID=your_github_client_id
SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_GITHUB_CLIENT_SECRET=your_github_client_secret
# URL de callback no Render: https://your-app.onrender.com/login/oauth2/code/github

# Configurações da aplicação
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# Flyway (migrations automáticas)
SPRING_FLYWAY_ENABLED=true
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true

# JPA/Hibernate
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_JPA_SHOW_SQL=false

# Logging
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_BR_COM_FIAP_MOTTU=INFO
